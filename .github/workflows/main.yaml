name: CI Main

on:
  push:
    branches: [main]

jobs:
  ci:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      image_tag: stable
      push_latest: false
      push_dev: false
    secrets:
      ghcr_token: ${{ secrets.GITHUB_TOKEN }}